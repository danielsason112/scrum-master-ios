# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Scrum Master' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Scrum Master
	pod 'Firebase/Auth'
	pod 'Firebase/Database'
  pod 'Kingfisher', '~> 6.0'
  
  target 'Scrum MasterTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Scrum MasterUITests' do
    # Pods for testing
  end

end
